本アプリケーションは空き物理メモリを確保して解放するだけのツールです。
ファイルキャッシュ等を物理メモリから追い出すことを目的(目標)としています。

第一引数には確保する最大メモリをMByte単位で指定できます。
省略した場合は空き物理メモリ分となります。
大きめに指定しても確保できるところまで確保して解放します。

空き物理メモリよりも大きな値を指定すると使われているメモリまでキャッシュアウトされるため、
システムが遅くなることに注意してください。

・ Visual C++ 2010のWin32/X64の双方でユニコードビルドでコンパイルされています。

